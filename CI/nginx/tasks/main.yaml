- name: install nginx
  apt:
          pkg:
                  - nginx
          state: latest
          update_cache: true


- name: add nginx.conf file
  template:
          src: ./nginx.conf
          dest: /etc/nginx/nginx.conf
  register: nginx_config





